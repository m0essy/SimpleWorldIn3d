package org.unlimited.astar

import org.specs2.mutable._
import org.specs2.matcher.ShouldExpectable
import org.specs2.internal.scalaz.Digit._0

class AstarSpec extends Specification {
//    "AStar" should {
//      "if there are no obstructions" in {
//        var map = new SimpleMap(5)
//        "find the direct path on a line" in {
//          val astar = new AStarSearch[Coordinate]()
//          val path = astar.search(map.getNodeAsSearchable(1,1), map.getNodeAsSearchable(1,4))
//          path.map {c:Coordinate => (c.x, c.y)} mustEqual (1,1) :: (1,2) :: (1,3) :: (1,4) :: Nil
//        }
//      }
//    }
  }

class SimpleMapSpec extends Specification {
//
//  "SimpleMap" should {
//    "return a searchable for a coordinate" in {
//      val map = new SimpleMap(5)
//      val node = map.getNodeAsSearchable(1,2)
//      node must beAnInstanceOf[Searchable[Coordinate]]
//    }
//
//    "throw an exception if the requested node is outside the boundaries of the map" in {
//      val map = new SimpleMap(5)
//
//      map.getNodeAsSearchable(6,1) must throwA[IndexOutOfBoundsException]
//      map.getNodeAsSearchable(-1,2) must throwA[IndexOutOfBoundsException]
//    }
//  }
//
}

